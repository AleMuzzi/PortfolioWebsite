version: '3.9'

services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3001'
    environment:
      - NODE_ENV=production
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - PORT=3001
    restart: unless-stopped
