version: '3.9'

services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '30013:3001'
    environment:
      - NODE_ENV=production
      - MINIMAX_API_KEY=${MINIMAX_API_KEY}
      - PORT=3001
    restart: unless-stopped
